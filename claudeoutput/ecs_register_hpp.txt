#pragma once

#include <flecs.h>

namespace engine::ecs {

// Component tags for entity classification
struct Movable {};
struct Debug {};

// Register all components and tags with the ECS world
void register_components(flecs::world& world);

} // namespace engine::ecs