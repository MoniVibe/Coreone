#include "ecs_register.hpp"

namespace engine::ecs {

void register_components(flecs::world& world) {
    // Register component tags
    world.component<Movable>("Movable");
    world.component<Debug>("Debug");
    
    // Additional component registration can be added here
    // The transform data itself lives in SoA, not as ECS components
}

} // namespace engine::ecs